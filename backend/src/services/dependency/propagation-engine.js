const {
  propagateMasteryGain,
  MAX_PROPAGATION_RATIO,
  MAX_PROPAGATION_PER_TOPIC,
  MAX_TOTAL_PROPAGATION_PER_DAY
} = require("./mastery-propagation");

module.exports = {
  propagateMasteryGain,
  MAX_PROPAGATION_RATIO,
  MAX_PROPAGATION_PER_TOPIC,
  MAX_TOTAL_PROPAGATION_PER_DAY
};
