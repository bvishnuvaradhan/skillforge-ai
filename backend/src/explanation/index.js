const {
  buildExplanation,
  formatExplanationForDisplay,
  getRecommendationTitle,
  getRecommendationIcon,
  getRecommendationColor
} = require("./basic");

module.exports = {
  buildExplanation,
  formatExplanationForDisplay,
  getRecommendationTitle,
  getRecommendationIcon,
  getRecommendationColor
};
